services:
  - type: web
    name: weichat-sync
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/main.py
    envVars:
      - key: PORT
        value: 8080
      - key: JWT_SECRET
        generateValue: true
