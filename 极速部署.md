# 🚀 极速部署 - 3步上线

## 为什么之前说复杂？
因为沙箱环境完全封锁了网络（不能联网、不能开端口），我只能准备好代码。
实际部署需要你在自己电脑上执行以下命令。

---

## 第1步：推送代码到 GitHub（1分钟）

打开终端，运行：

```bash
cd /Users/mac/Documents/Codex/2026-09-05/n/outputs/weichat-sync

# 创建GitHub仓库（在github.com上新建一个空仓库，记下仓库地址）
git remote add origin https://github.com/你的用户名/weichat-sync.git
git push -u origin main
```

---

## 第2步：一键部署到 Railway（1分钟）

```bash
# 安装 Railway CLI
npm install -g @railway/cli

# 登录（会打开浏览器让你授权）
railway login

# 关联项目并部署（在 Railway 网页上选择你刚创建的仓库）
railway link
railway up -d
```

或者直接访问 https://railway.app → New Project → Deploy from GitHub → 选 weichat-sync

---

## 第3步：获取网址，发给别人！

部署成功后 Railway 会显示类似：
```
https://weichat-sync-xxxx.up.railway.app
```

把 **网址 + admin + admin123** 发给对方即可使用。

---

## ⚡ 最懒人方案（如果你不想用命令行）

1. 打开 https://railway.app ，用 GitHub 登录
2. 点击 New Project → Deploy from GitHub repo → 选 weichat-sync
3. 等待构建完成，自动获得网址
4. 在 Settings → Variables 添加 `JWT_SECRET=abc123`（随便填）

**全程不需要写任何代码，点鼠标就行。**
