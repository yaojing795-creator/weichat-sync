# 🚀 云端微信跟圈 SaaS - 一键部署到公网

## 方案一：Railway（推荐，最简单）

### 第1步：推送到 GitHub
```bash
cd /Users/mac/Documents/Codex/2026-09-05/n/outputs/weichat-sync
git init
git add .
git commit -m "初版：微信跟圈SaaS系统"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/weichat-sync.git
git push -u origin main
```

### 第2步：部署到 Railway
1. 打开 https://railway.app
2. 登录（支持 GitHub 账号）
3. 点击 **New Project** → **Deploy from GitHub repo**
4. 选择刚推送的 `weichat-sync` 仓库
5. Railway 自动检测到 Python 项目并开始构建
6. 构建完成后，在 Settings → Variables 中添加：
   - `JWT_SECRET` = 随便填一个随机字符串（如 `my-secret-key-12345`）

### 第3步：获取访问地址
- 部署成功后，Dashboard 会显示一个地址：`https://weichat-sync-xxxx.up.railway.app`
- **直接把这个网址 + 管理员账号密码给别人即可使用！**

---

## 方案二：Render（免费，稳定）

### 第1步：推送到 GitHub（同上）

### 第2步：部署到 Render
1. 打开 https://render.com
2. 登录 → 点击 **New +** → **Web Service**
3. 连接 GitHub 仓库
4. 填写配置：
   - **Name**: weichat-sync
   - **Environment**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python backend/main.py`
   - **Instance Type**: Free
5. 添加环境变量：
   - `PORT` = `8080`
   - `JWT_SECRET` = `your-random-secret`

### 第3步：获取访问地址
- 部署完成后获得：`https://weichat-sync.onrender.com`

---

## 方案三：腾讯云/阿里云（生产级）

### 快速启动
```bash
# 1. 在服务器上克隆代码
git clone <你的GitHub仓库> && cd weichat-sync

# 2. 安装依赖
pip3 install -r requirements.txt

# 3. 启动服务
PORT=8765 python3 backend/main.py &

# 4. 配置 nginx 反向代理（参考 README.md）
```

---

## 登录账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | `admin` | `admin123` |
| 普通用户 | 自行注册 | 自设 |

⚠️ **首次登录后请立即修改 admin 密码！**

---

## 功能验证清单

部署后请依次测试：
1. ✅ 用 admin/admin123 登录
2. ✅ 仪表盘显示统计数据
3. ✅ 添加一个测试微信账号
4. ✅ 让账号上线（点击"上线"按钮）
5. ✅ 创建跟圈任务（监听某个好友）
6. ✅ 启动任务，查看状态变为"运行中"
7. ✅ 等待几十秒后查看转发日志
8. ✅ 注册一个新用户，验证权限隔离
